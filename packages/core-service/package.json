{
  "name": "@idealjs/ponder-core-service",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "generate:dev": "openssl req -x509 -newkey rsa:2048 -nodes -sha256 -subj '/CN=localhost' -addext 'subjectAltName = DNS.1:localhost' -keyout server.key -out server.crt",
    "dev": "nodemon"
  },
  "devDependencies": {
    "@types/koa__router": "^12.0.0",
    "@types/minio": "^7.0.15",
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@idealjs/ponder-server-shared": "^0.1.0",
    "@koa/router": "^12.0.0",
    "koa": "^2.14.1"
  }
}
